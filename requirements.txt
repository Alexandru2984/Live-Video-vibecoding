# Pinned for reproducible deploys (versions running in production).
# Upgrade deliberately: bump here, run the test suite, then deploy.
Django==6.0.5
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.1
redis==7.4.0
asgiref==3.11.1
# Optional error tracking: inert unless SENTRY_DSN is set (see settings.py).
sentry-sdk==2.66.0
